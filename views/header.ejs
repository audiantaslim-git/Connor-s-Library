<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Connor's Book Library</title>
    <link rel="stylesheet" href="main.css" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Lora:ital,wght@0,400..700;1,400..700&family=Merriweather:ital,opsz,wght@0,18..144,300..900;1,18..144,300..900&display=swap"
      rel="stylesheet"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Open+Sans:ital,wght@0,300..800;1,300..800&display=swap"
      rel="stylesheet"
    />
  </head>
  <body class="bg-[#f5f2f0]">
    <nav
      class="fixed top-0 left-0 w-full shadow-lg p-4 flex items-center bg-[#faf0eb]"
      style="box-shadow: 0 4px 6px rgba(0,0,0,0.05)"
    >
      <!-- Logo -->
      <div class="flex items-center mr-auto">
        <a href="/home" class="no-underline">
          <img src="images/connors_library.svg" alt="Logo" class="h-8 w-auto" />
        </a>
      </div>

      <!-- Search Bar -->
      <div class="px-4 flex-1 max-w-xl">
        <form action="/search" method="GET">
          <input
            type="text"
            name="searchQuery"
            placeholder="Search books title or author..."
            class="font-opensans bg-white w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-1 focus:ring-[#f2d1bd] border-[#f2d1bd]"
          />
        </form>
      </div>

      <!-- Menu Items -->
      <div class="flex items-center ml-auto space-x-5 mr-[40px]">
        <a
          href="/"
          class="<%= activePage === 'home' ? 'text-[#594A42] font-opensans-bold' : 'font-opensans text-[#80695d]' %>"
          >Home</a
        >
        <a
          href="/search?searchQuery=Mitch+Albom"
          class="<%= activePage === 'search' ? 'text-[#594A42] font-opensans-bold' : 'font-opensans text-[#80695d]' %>"
        >
          Search Result</a
        >
        <a href="#" class="text-[#80695d]">Reviews</a>
      </div>
    </nav>
  </body>
</html>
